Pushed a follow-up commit (`25c107c`) that drops the literal `${{ github.repository }}` from the `boilerplate_repository` input description. GitHub Actions parses `${{ ... }}` as an expression in *any* string in a workflow file (including `description:`), so the previous commit was breaking the workflow's own syntax check. No behavior change — the input and its default are the same.
