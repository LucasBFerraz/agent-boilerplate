# Examples

- [`consumer-repo-workflow.yml`](consumer-repo-workflow.yml) — Drop this into a consumer repo to wire up the boilerplate's three reusable workflows.
- [`agent-prompt-template.md`](agent-prompt-template.md) — The default prompt template the boilerplate synthesizes, plus two ways to customize it.

Looking for more? See the [`docs/`](../docs/) folder.
